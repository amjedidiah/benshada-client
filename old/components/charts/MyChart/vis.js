import * as d3 from 'd3';

const draw = () => {
  d3.select('.vis-mychart > *').remove();
  // const { data } = props;
  // const { width } = props;
  // const { height } = props;
  // const svg =
  // d3.select('.vis-mychart').append('svg').attr('width', width).attr('height', height);
  // TODO: draw the chart
};

export default draw;
